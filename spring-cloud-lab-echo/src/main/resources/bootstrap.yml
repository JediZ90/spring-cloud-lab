server:
  port: 8001

spring:
  application:
    name: spring-cloud-lab-echo
  cloud:
    config:
      discovery:
        enabled: true
        serviceId: spring-cloud-lab-config

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:5002/eureka/